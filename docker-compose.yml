version: '2'
services:
  app:
    build: .
    image: damoti/shipmaster
    ports:
      - "8000:8000"
    volumes:
      - /var/lib/shipmaster:/var/lib/shipmaster
      - /var/run/docker.sock:/var/run/docker.sock

